name: Gatsby Publish

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: enriikke/gatsby-gh-pages-action@v2
        with:
          # Pavels personal token
          access-token: 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
